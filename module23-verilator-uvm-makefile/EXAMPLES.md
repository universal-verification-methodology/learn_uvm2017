# Examples — Verilator + UVM Makefile knobs

Track A only. Use the **in-course** tree (combined archive tree is ignored).

## Prompts

1. Restate what `UVM_HOME` must point at (`uvm_pkg.sv` lives where?).
2. Open [`../examples/verilator-uvm-hello/Makefile`](../examples/verilator-uvm-hello/Makefile) and name `SIM`, `TEST`, and `UVM_TESTNAME`.
3. Run `make dry-run` from `examples/verilator-uvm-hello` and note pass/fail of the path checks.

## Stretch

When dry-run is green, continue to module 24 for a full `make run`.
