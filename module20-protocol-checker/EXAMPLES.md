# Module 20 examples — Protocol checker

Track A (real UVM literacy). Browser lab is shipped.

## Prompts

1. Read `examples/checker-sketch/checker.txt` and restate why **protocol checker** and **scoreboard** answer different questions.
2. Sketch three valid/ready rules: handshake drop, data hold, no X when valid.
3. Give one example where all protocol rules pass but scoreboard would fail.

## Stretch

Open `protocol-checker`, confirm starter PASS, then Demo HOLD fail / drop valid.
