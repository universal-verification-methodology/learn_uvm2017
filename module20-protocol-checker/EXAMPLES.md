# Module 20 examples — Protocol checker

Track A (real UVM literacy). Pair with Track B `protocol-checker` when you want the browser sketch.

## Prompts

1. Restate why a **protocol checker** and a **scoreboard** answer different questions in one sentence.
2. Sketch three rules (handshake drop, data hold while stalled, no X when valid) on paper.
3. Point to where this appears in [`../learn_uvm2017_sv_verilator/`](../learn_uvm2017_sv_verilator/) if you can find it.

## Stretch (optional)

Redo the same idea with the `protocol-checker` starter (clean beat PASS, then Demo HOLD fail / drop valid).
