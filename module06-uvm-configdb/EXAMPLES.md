# Module 06 examples — ConfigDB

Track A (real UVM literacy). Browser lab is shipped.

## Prompts

1. Read `examples/configdb-sketch/set-get.txt` and restate the core idea in one sentence.
2. Sketch set on env.agent for vif and get from env.agent.drv — show the walk-up.
3. Explain why a get for `is_active` misses when only `vif` was set.

## Stretch

Open `uvm-configdb`, try more-specific-wins and sibling_miss presets, then grep `uvm_config_db` in the legacy course.
