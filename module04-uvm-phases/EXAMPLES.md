# Module 04 examples — UVM phases

Track A (real UVM literacy). Browser lab is shipped.

## Prompts

1. Read `examples/phase-timeline/phases.txt` and restate the core idea in one sentence.
2. Sketch the five core phases on paper with build/connect direction arrows.
3. Explain why an objection in run keeps simulation alive.

## Stretch

Open `uvm-phases`, load starter, walk all core phases, then grep `build_phase` in the legacy course.
