# Module 17 examples — Virtual sequence

Track A (real UVM literacy). Pair with Track B `uvm-vseq` when you want the browser sketch.

## Prompts

1. Restate **virtual sequencer vs virtual sequence** in one sentence each.
2. Sketch UART + SPI refs and contrast sequential vs parallel start.
3. Point to where this appears in [`../learn_uvm2017_sv_verilator/`](../learn_uvm2017_sv_verilator/) if you can find it.

## Stretch (optional)

Redo the same idea with the `uvm-vseq` starter (sequential UART→SPI, then Demo parallel / missing-ref fail).
