# Module 11 examples — Plusargs / testname

Track A (real UVM literacy). Pair with Track B `uvm-plusargs` when you want the browser sketch.

## Prompts

1. Restate what **`+UVM_TESTNAME`** does versus a custom plusarg knob in one sentence each.
2. Sketch a sim command line with testname, seed, verbosity, and one custom knob.
3. Point to where this appears in [`../learn_uvm2017_sv_verilator/`](../learn_uvm2017_sv_verilator/) if you can find it.

## Stretch (optional)

Redo the same idea with the `uvm-plusargs` starter (base_test + SEED, then Demo no testname / unknown test).
