# Module 11 examples — Plusargs / testname

Track A (real UVM literacy). Browser lab is shipped.

## Prompts

1. Read `examples/plusargs-sketch/cmdline.txt` and restate `+UVM_TESTNAME` vs a custom knob in one sentence each.
2. Sketch a sim command with testname, seed, verbosity, and one project plusarg.
3. Explain what fails when testname is missing versus when a custom knob is missing.

## Stretch

Open `uvm-plusargs`, try no-testname and unknown-test presets, then grep `get_arg_value` in the legacy CLP example.
