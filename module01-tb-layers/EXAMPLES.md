# Module 01 examples — TB layers

Track A (real UVM literacy). Browser lab is shipped.

## Prompts

1. Read `examples/layer-sketch/hierarchy.txt` and restate the core idea of **TB layers** in one sentence.
2. Sketch the stimulus and observe paths on paper (test → env → agent → … → DUT).
3. Optionally skim the architecture example in [`../learn_uvm2017_sv_verilator/`](../learn_uvm2017_sv_verilator/) module six.

## Stretch (optional)

Redo the same idea with the starter challenges in the browser lab.
