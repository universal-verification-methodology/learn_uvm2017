# Module 21 examples — VIP anatomy

Track A (real UVM literacy). Pair with Track B `vip-anatomy` when you want the browser sketch.

## Prompts

1. Restate the four VIP deliverables (**agent**, **checker**, **coverage**, **docs**) in one sentence each.
2. Sketch a UART VIP package tree on paper and mark what is missing if you only ship an agent.
3. Point to where this appears in [`../learn_uvm2017_sv_verilator/`](../learn_uvm2017_sv_verilator/) if you can find it.

## Stretch (optional)

Redo the same idea with the `vip-anatomy` starter (complete package, then Demo incomplete / missing docs).
