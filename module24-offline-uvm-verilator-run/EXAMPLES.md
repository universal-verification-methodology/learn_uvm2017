# Examples — Run UVM on Verilator

Track A only. In-course tree: [`../examples/verilator-uvm-hello/`](../examples/verilator-uvm-hello/).

## Prompts

1. Restate why browser UVM sketches cannot replace this compile.
2. Run `make run SIM=verilator` from `examples/verilator-uvm-hello` with `UVM_HOME` set.
3. Paste the first UVM report / pass-fail line into your notes.

## Stretch

Compare compile time cold vs second run after `make clean` vs without clean. Note one Verilator warning you chose to ignore (`-Wno-*`).
