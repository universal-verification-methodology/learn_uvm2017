Run UVM on Verilator — offline sketch
=====================================

Prerequisites:
 Module 22 — Verilator as UVM host (limits)
 Module 23 — UVM_HOME, SIM=verilator, TEST= / +UVM_TESTNAME

Goal:
 Build and run one UVM test with Verilator as host
 (Accellera UVM 2017)

Practice surface:
 courses/learn_uvm2017/examples/verilator-uvm-hello/

Minimal flow:
 1. cd courses/learn_uvm2017/examples/verilator-uvm-hello
 2. export UVM_HOME=/path/to/1800.2-2017-1.0/src
 3. make dry-run
 4. make run SIM=verilator

What you should see:
 Verilator compile of uvm_pkg.sv + DUT + test
 binary run with +UVM_TESTNAME=…
 UVM info / report summary — capture pass or fail

Notes:
 First UVM+Verilator compile can take several minutes
 Browser sketches ≠ simulator pass
 Record Verilator version + UVM version in your notes

Common mistakes:
 UVM_HOME unset
 make from wrong directory
 skipping module 23 Makefile map
 expecting instant compile on cold cache
 treating platform HTML as the offline run
