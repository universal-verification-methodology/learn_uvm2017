# Module 05 examples — Factory

Track A (real UVM literacy). Browser lab is shipped.

## Prompts

1. Read `examples/factory-sketch/overrides.txt` and restate the core idea in one sentence.
2. Sketch a type override and an instance override for swapping base_driver → error_driver.
3. Explain why instance override wins when both apply at the same path.

## Stretch

Open `uvm-factory`, try the agent VIP swap preset, then grep `set_type_override` in the legacy course.
